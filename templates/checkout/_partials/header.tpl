{block name='header'}
  <header id="header" class="templates--checkout--_partials--header">
    <div class="header-bottom">
      <div class="container">
          {block name='header_logo'}
            <a class="header-logo " href="{$urls.base_url}" title="{$shop.name}">
              <img src="{$shop.logo}" alt="{$shop.name}">
            </a>
          {/block}
      </div>
    </div>
  </header>
{/block}
