{block name='cart_summary_items_subtotal'}
  <div id="items-subtotal" class="templates--checkout--_partials--cart-summary-items-subtotal">
    <span class="js-subtotal subtotal">{$cart.summary_string}</span>
    <span class="total">{$cart.totals.total.amount}</span>
  </div>
{/block}
