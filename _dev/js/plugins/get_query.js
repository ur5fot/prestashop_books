// Parse URL Queries Method
/*
jQuery.getQuery = function (query) {
        // console.log(this);
        /!* query = query.replace(/[\[]/,"\\\[").replace(/[\]]/,"\\\]");
         var expr = "[\\?&]"+query+"=([^&#]*)";
         var regex = new RegExp( expr );
         var results = regex.exec( this.href );
         if( results !== null ) {
         return results[1];
         return decodeURIComponent(results[1].replace(/\+/g, " "));
         } else {
         return false;
         }*!/
    };*/
